# 0.8 — Inputs, group heads, projection and Windows build path

This version supersedes0.7 in the following behaviors:

- Enter/blur/TextField draft state replaces0.28-second numeric debounce. No ordinary parameter/SOURCE Apply button.
- Comments produce no opaque notices; original text retained, real diagnostics folded.
- Default Sky heads/hit by earliest note per explicitgroup, not geometric region-only. Newgroup and transactional authoring inheritance added.
- Active point placement cancels before radial menu; completed note keeps Point mode.
- UI size/content width adjusted, playback icon integrated, rates clickcycle, compact difficulty tabs, one-decimal scroll.
- Added faint central-floor projections and rational1/N air grid.
- New nativeWindows dialogs/local audio loader behind verified build define; local BuildPlayer workflow and PowerShell wrapper.
- No camera/track/Flick-height/score model/manual-save policy rewrite.

See TEST_REPORT for actually executed independent tests. No compiledexe or Unityruntime test was produced in this environment.
