# 0.2 更新摘要

- Flick使用同深度固定参考单位的投影尺度，彻底断开音符宽度与高度；极窄端帽只限横向skew。
- 新增稳定文档ID、事务预览/提交/取消、群组编辑、粘贴/group策略、镜像、续段。
- 新增运行时鼠标捕获、三维放置/拖动/边界把手/框选、多选和快捷键。
- 新增单调时间编辑面板、track忽略布局、波形、书签与循环。
- 新增本地WAV解码，Editor OGG/MP3走音频导入器，不再因可选Networking模块阻止默认编译。
- 新增工作副本、.bak、回读校验、恢复副本、会话侧车文件。
- 新增连续最小宽度诊断、C#编辑/WAV测试、独立Python/静态检查、合成验收样本。
- 保留已经认可的相机、场地、场景、Shader和原谱面；不带旧unitypackage，不替换项目配置。

本版没有Unity/C#实际编译运行结果；各检查的具体范围见TEST_REPORT。
